# SH-DATASET — requirements index
#
# Install only what you need:
#   pip install -r requirements/base.txt    # runtime (end users)
#   pip install -r requirements/test.txt   # runtime + tests (CI, contributors)
#   pip install -r requirements/dev.txt    # runtime + tests + linting (maintainers)
#
# Or use the extras defined in pyproject.toml:
#   pip install -e .[test]
#   pip install -e .[dev]
#

-r base.txt
